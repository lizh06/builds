( ./configure --prefix=/v/campus/vi/cs/credit/lizheng/.rs \
  --enable-locked-deps --enable-ccache --enable-ninja \
  --enable-optimize --enable-optimize-llvm \
  --enable-llvm-assertions --enable-debug-assertions \
  --enable-emscripten --enable-profiler --enable-sanitizers \
  --enable-experimental-parallel-queries \
  --release-channel=stable && make -j $JNUM ) 2>&1 | tee build.log

# --enable-extended \
# --enable-full-tools \
