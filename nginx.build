( CC=gcc CFLAGS=-g ./auto/configure --prefix=$S2 \
  --with-http_dav_module --add-module=../nginx-dav \
  --add-module=../nginx-upload &&
  make -j $CC_LEVEL && make install && make clean
) 2>&1 | tee build.log

